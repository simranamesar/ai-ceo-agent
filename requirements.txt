# --- core ---
pyyaml
python-dotenv

# --- data collection ---
requests
feedparser
beautifulsoup4

# --- processing & store ---
sentence-transformers
transformers          # sentiment classifier (encoder)
accelerate            # required for device_map='auto' (local LLM on GPU)
chromadb

# --- llm (OpenAI-compatible client -> Ollama / self-hosted vLLM/TGI) ---
openai

# --- agent framework (Graph 1 pipeline + Graph 2 ReAct loop) ---
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.2

# --- dashboard ---
streamlit
plotly
pandas
